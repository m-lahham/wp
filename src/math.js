const math = {
    add: (a,b)=>{return a + b;},
    subtract : (a,b)=>{return a - b;},
    multiply : (a,b)=>{return a * b;}
    }

export function addFunction(a,b){return a + b;};
export function subtractFunction(a,b){return a - b;};
export default math;